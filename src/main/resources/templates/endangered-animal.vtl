<h2>$endangered-animal.getName()</h2>


<ul>
  #foreach($sighting in $endangered-animal.getSightings())
    <li><a href="/sightings/$sighting.getId()">$sighting.getLocation()</a><li>
  #end
</ul>

<h2>Edit this Endangered Animal:</h2>

<form action="/endangered-animals/$endangered-animal.getId()/update" method="post">
  <label for="endangered-animal-name">Species Update:</label>
  <input id="endangered-animal-name" name="endangered-animal-name" type="text" required>
  <br>
  <label for="health">Update Health:</label>
  <select id='health' name='health' type='text' required>
    <option value=ILL>Ill</option>
    <option value=OKAY>Okay</option>
    <option value=WELL>Well</option>
  </select>
  <br>
  <label for="animal-age">Add Animal's Age:</label>
  <select id='animal-age' name='animal-age' type='text' required>
    <option value=NEWBORN>Newborn</option>
    <option value=YOUNG>Young</option>
    <option value=ADULT>Adult</option>
  </select>
  <button type="submit" class="btn btn-danger">Update Animal</button>
</form>

<h2>Delete This Animal:</h2>

<form action="/endangered-animals/$endangered-animal.getId()/delete" method="post">
  <button type="submit" class="btn btn-danger" id="delete-endangered-animal">Delete Animal</button>
</form>
